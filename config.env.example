# Настройки для парсера
USER_AGENT_ROTATE=true
REQUEST_TIMEOUT=10
MAX_RETRIES=3
DELAY_BETWEEN_REQUESTS=2

# Настройки базы данных PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=avito_data
DB_USER=postgres
DB_PASSWORD=your_password

# Настройки для CSV
CSV_DIRECTORY=./data
CSV_FILENAME_TEMPLATE=avito_data_{date}.csv 